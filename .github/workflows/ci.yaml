name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: |
          cd core && cargo fmt --check
          cd ../cli && cargo fmt --check
          cd ../server && cargo fmt --check

      - name: Clippy
        run: |
          cd core && cargo clippy -- -D warnings
          cd ../cli && cargo clippy -- -D warnings
          cd ../server && cargo clippy -- -D warnings

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build all crates
        run: |
          cd core && cargo build --release
          cd ../cli && cargo build --release
          cd ../server && cargo build --release

      - name: Upload CLI binary
        uses: actions/upload-artifact@v4
        with:
          name: planspec-cli
          path: cli/target/release/planspec

      - name: Upload server binary
        uses: actions/upload-artifact@v4
        with:
          name: planspec-server
          path: server/target/release/planspec-server

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Run unit tests
        run: |
          cd core && cargo test
          cd ../server && cargo test

  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Download CLI binary
        uses: actions/download-artifact@v4
        with:
          name: planspec-cli
          path: cli/target/release/

      - name: Download server binary
        uses: actions/download-artifact@v4
        with:
          name: planspec-server
          path: server/target/release/

      - name: Make binaries executable
        run: |
          chmod +x cli/target/release/planspec
          chmod +x server/target/release/planspec-server

      - name: Start server
        run: |
          server/target/release/planspec-server &
          sleep 3
          curl -s http://localhost:8080/health || exit 1

      - name: Run integration tests
        run: cd cli && cargo test --test integration_test -- --test-threads=1

      - name: Stop server
        if: always()
        run: pkill planspec-server || true

  validate-fixtures:
    name: Validate Fixtures
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Download CLI binary
        uses: actions/download-artifact@v4
        with:
          name: planspec-cli
          path: cli/target/release/

      - name: Make CLI executable
        run: chmod +x cli/target/release/planspec

      - name: Validate fixture bundle
        run: cli/target/release/planspec validate -f docs/planspec/fixtures/v0.1.0/bundle.yaml

  schema-validation:
    name: Schema Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ajv-cli
        run: npm install -g ajv-cli ajv-formats

      - name: Validate JSON schemas are valid
        run: |
          for schema in schemas/v1alpha1/*.schema.json; do
            echo "Validating $schema..."
            ajv compile -s "$schema" --spec=draft2020 -r "schemas/common/*.schema.json" || exit 1
          done
